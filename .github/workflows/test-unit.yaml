name: test-unit

on:
  workflow_call:

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      # Use the version from go.mod (1.25). Pinning an older version made
      # GOTOOLCHAIN=auto download the minimal go1.25 toolchain module, which
      # lacks the `covdata` tool and broke `go test -coverprofile` on the
      # example main packages.
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: go build -v ./...

      - name: Run tests
        shell: bash
        run: go test -v -race -count=1 -coverprofile=coverage.txt -covermode=atomic ./...

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        if: matrix.os == 'ubuntu-latest'
        with:
          files: ./coverage.txt
          flags: unittests
